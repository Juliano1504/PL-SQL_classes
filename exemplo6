DECLARE
NM_DPTO VARCHAR2(50);
BEGIN
    FOR ND IN 1..5
    LOOP
        IF ND = 3 THEN
        SELECT NM_DPTO
        INTO NM_DPTO
        FROM PRO_DEPTO 
        WHERE COD_DPTO = 3; 
        DBMS_OUTPUT.PUT_LINE(NM_DPTO);
    END IF;
    END LOOP;
END;

SELECT * FROM PRO_DEPTO
