create or replace NONEDITIONABLE FUNCTION FC_BUSCA_CIDADE (P_CODIGO IN NUMBER)

RETURN VARCHAR2

IS
V_NOME VARCHAR2(50);
BEGIN
    SELECT C.nmcidade
        INTO V_NOME
        FROM PRO_CIDADE C
        WHERE C.cdcidade = P_CODIGO;
    RETURN(V_NOME);
END;
